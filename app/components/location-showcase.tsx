'use client';

import { useState } from 'react';
import { MapPinIcon, StarIcon, ClockIcon, PhotoIcon } from '@heroicons/react/24/outline';

interface LocationData {
    id: string;
    name: string;
}